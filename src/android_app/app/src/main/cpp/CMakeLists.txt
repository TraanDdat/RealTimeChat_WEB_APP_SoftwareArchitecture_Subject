cmake_minimum_required(VERSION 3.22.1)

project("zaloclone")

add_library(
        zaloclone
        SHARED
        native-lib.cpp)

find_library(
        log-lib
        log)

target_link_libraries(
        zaloclone
        ${log-lib})
